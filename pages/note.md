- `=` 赋值
- `==` 相等运算
- `Boolean` 逻辑陈述或运算
- `Name()` 函数名
- `SubClass(Class)` 继承
-
- `wW` 是非字母数字下划线
  `sS` 是非Spc, Tab, Etr
  `[＾]` 定义取反字符集 内部不转义
  `＄` 开始结束以
-
- `dt` delta 增量
- `Upper Title` 通常是不变化的
-
- `render` 描绘，表示为